name: Sensors — LiDAR RPLiDAR

on:
  push:
    branches: [main, dev, 'feat/**', 'fix/**', 'refactor/**', 'chore/**', 'perf/**']
    paths:
      - 'sensors/lidar-rplidar/**'
      - '.github/workflows/sensors-lidar-rplidar.yml'
      - '.github/workflows/_sensor-docker.yml'
  workflow_dispatch:
    inputs:
      no-cache:
        description: 'Build without Docker layer cache'
        type: boolean
        default: false

concurrency:
  group: sensors-lidar-rplidar-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

jobs:
  build:
    uses: ./.github/workflows/_sensor-docker.yml
    with:
      image: lidar-rplidar
      context: ./sensors/lidar-rplidar
      no-cache: ${{ inputs.no-cache == true }}
