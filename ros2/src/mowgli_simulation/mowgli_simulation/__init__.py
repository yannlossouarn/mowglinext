# mowgli_simulation Python package — Webots driver plugins.
#
# Plugins are loaded by webots_ros2_driver via `<plugin
# type="mowgli_simulation.module.Class">` in the URDF. They run inside
# the driver process and have direct access to the Webots Supervisor
# API (when the Robot is supervisor=TRUE).
